# Optional: set the exact backend endpoint (most reliable across PCs)
# Example:
# VITE_API_URL=http://localhost/SysArchSATORRE/SitInSatorre/server/authenticate.php

# Optional: set only the backend path if host/port should be auto-detected
# Example:
# VITE_API_PATH=/SysArchSATORRE/SitInSatorre/server/authenticate.php
